SECTION .text
CHARS #hw "Hello, World!"

SECTION .code
MOV $SYS32, 1 // FD=1 (stdout)
MOV $SYS33, #hw // DATA=hw
MOV $SYS34, 13 // LEN=13
SYSCALL $R0, 1, 0 // write

MOV $SYS32, 0 // exit code 0
SYSCALL $R0, 60, 0 // exit
